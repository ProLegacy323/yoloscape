# modes
#------------------
# cachePacking will grab all numbered .gz files in the /cache/Music directory and pack them into index3.
# Please keep in mind, if you have a file names 1.gz in there, it will override the MIDI in index position 1.
# This is the same for any number, so to add new music, find a null index position.
MIDI_PACKING=false